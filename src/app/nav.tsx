import Image from "next/image";

export default function Nav() {
    return (
        <nav>
            <a href="/" className="nav-logo">
                <p>Sale Savant</p>
            </a>
        </nav>
    );
}
